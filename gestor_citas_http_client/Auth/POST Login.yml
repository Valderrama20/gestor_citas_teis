info:
  name: POST Login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:9001/auth/login
  body:
    type: json
    data: |-
      {
        "email": "admin@iesteis.es",
        "password": "abc123."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
