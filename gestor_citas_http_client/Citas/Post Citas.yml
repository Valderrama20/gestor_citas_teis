info:
  name: Post CIta
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:9001/cita
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
